# TODO

- [ ] Update `src/utils/jwt.util.js` để validate `ACCESS_TOKEN_SECRET` và `REFRESH_TOKEN_SECRET` trước khi gọi `jwt.sign`, ném lỗi rõ ràng nếu thiếu.
- [ ] (Sau khi sửa) Chạy lại server và test login để xác nhận JWT được generate thành công.

